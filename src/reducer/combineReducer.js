import IncDec from "./IncDec";

import { combineReducers } from "redux";

const rootReducer = combineReducers({
  IncDec
}); // can add multiple reducer func. here

export default rootReducer;
